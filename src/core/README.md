# Core

Phase 0 keeps all persistent data in the shared core model. Workflow modes may
create or update documents, pages, layers, templates, linked groups, and batch
jobs, but they do not own independent canvas, text, image, save, or export
systems.
