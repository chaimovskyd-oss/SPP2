# Services

Business services translate workflows into shared core operations. They should
call Python only through IPC-backed bridge wrappers when heavy image, product,
or print processing is needed.
