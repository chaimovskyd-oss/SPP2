# Template Engine

Templates describe pages, slots, text zones, locked layers, editable layers,
auto-fill rules, smart-arrange rules, and print specs. They create or update the
shared core model rather than owning a separate design format.
